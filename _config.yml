# Site-wide settings
title: Site title

# Build settings
remote_theme: jekyll/minima
plugins:
  - jekyll-feed
  - jekyll-seo-tag

# Minima-specific settings
minima:
  skin: auto
  nav_pages:
    - index.md
  hide_site_feed_link: true
